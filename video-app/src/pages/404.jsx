const NotFound = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <h1>This video doesn't exist!</h1>
    </div>
  );
};

export default NotFound;
